alias gst='git status'
alias gb='git branch'
alias gl='git pull'
alias gp='git push'

# assume-unchanged
# List files that are marked as assume-unchanged
alias ls-unchanged='git ls-files -v | grep "^[[:lower:]]"'
