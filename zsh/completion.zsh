# Enable autocomplete
autoload -U compinit; compinit

# Enable menu with arrows
# NOTE - disabling this enables a menu?
#zstyle ':completion:*' menu select

# Autocomplete CLI switches
setopt completealiases
